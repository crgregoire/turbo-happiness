import { AppRegistry } from 'react-native';
import App from './src/app';

// Starting point for our app
// Registers the name of our app from the appKey and mounts app.js

AppRegistry.registerComponent('tech_stack', () => App);
